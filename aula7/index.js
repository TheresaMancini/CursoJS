const nome = 'João';
// depois de declarado não pode alterar o valor. se fizer nome='Otavio' dará erro. 

// agora trabalhando com números

// JS é tipagem dinamica, como python. 

// fazer strign + number gera uma concatenação dos nomes (num console.log)

const primeiroNumero = 5;
const segundoNumero = 10;

const resultado = primeiroNumero * segundoNumero; 

let resultadoDuplicado = resultado * 3
console.log(resultado)
console.log(resultadoDuplicado)
